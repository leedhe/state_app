<div class="footer-push">
  <div class="panel panel-default col-md-6 col-md-offset-3">
        <!-- Default panel contents -->
    <div class="panel-heading"><h1>States</h1></div>
        <!-- Table -->
        <table class="table">
          <thead>
            <tr>
              <th>State</th>
              <th>Abbreviation</th>
              <th></th>
              <th></th>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @states.each do |state| %>
            <tr>
              <td><%= state.name %></td>
              <td><%= state.abbreviation %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <div class="thumbnail col-md-6 col-md-offset-3" style="margin-top: -12px;">
      <h3><a href="/search_states">Search another state &rarr;</a></h3>
    </div>
</div>
